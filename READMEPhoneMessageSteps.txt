TODO:
1. login to expo account on expo cli(command line) within the project
1.1 my command was "npx expo login -u YOUR-USERNAME-OR-EMAIL -p YOUR-PASSWORD"

2. to test goto: https://expo.dev/notifications
2.1 have your project running 
2.2 once a phone connects there will be a log with message "recipent ExponentPushToken = (PHONE TOKEN)"
2.3 copy that token and use it in the expo.dev/notifications tab and fill in the other info

3. Get message on phone??? (i was logged in on phone with same account)
